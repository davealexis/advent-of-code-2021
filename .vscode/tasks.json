{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Nim Project",
            "type": "shell",
            "command": "nim",
            "args": [
                "c",
                // "-o:${fileNoExtension}.exe",
                // "-d:debug",
                // "--debugger:native",
                // "-r",
                "${file}"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}